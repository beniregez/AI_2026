%% General definitions
\documentclass{article} %% Determines the general format.
\usepackage{a4wide} %% paper size: A4.
\usepackage[utf8]{inputenc} %% This file is written in UTF-8.
%% Some editors on Windows cannot save files in UTF-8.
%% If there is a problem with special characters not showing up
%% correctly, try switching "utf8" to "latin1" (ISO 8859-1).
\usepackage[T1]{fontenc} %% Format of the resulting PDF file.
\usepackage{fancyhdr} %% Package to create a header on each page.
\usepackage{lastpage} %% Used for "Page X of Y" in the header.
                      %% For this to work, you have to call pdflatex twice.
\usepackage{enumerate} %% Used to change the style of enumerations (see below).

\usepackage{amssymb} %% Definitions for math symbols.
\usepackage{amsmath} %% Definitions for math symbols.

\usepackage{tikz}  %% Pagacke to create graphics (graphs, automata, etc.)
\usetikzlibrary{shapes,arrows,calc,positioning}
\usetikzlibrary{graphs}
\usetikzlibrary{automata} %% Tikz library to draw automata
\usetikzlibrary{arrows}   %% Tikz library for nicer arrow heads
\usetikzlibrary{positioning}    %% Tikz library for more positioning options

\usepackage{hyperref} %% hyperlinks

\usepackage{chessfss} % contained in debian package texlive-games

\usepackage{listings}   % include & display python code
\usepackage[edges]{forest}  % for search trees

\tikzset{WhiteSquare/.style = {shape=rectangle, minimum width=28, minimum
    height=28, anchor=south west}}
\tikzset{BlackSquare/.style = {shape=rectangle, fill=lightgray, minimum
    width=28, minimum height=28, anchor=south west}}

%% Left side of header
\lhead{\course\\\semester\\Exercise \homeworkNumber}
%% Right side of header
\rhead{\authorname\\Page \thepage\ of \pageref{LastPage}}
%% Height of header
\usepackage[headheight=36pt]{geometry}
%% Page style that uses the header
\pagestyle{fancy}

\newcommand{\authorname}{Benjamin Regez\\Luis Tritschler}
\newcommand{\semester}{Spring Semester 2026}
\newcommand{\course}{Foundations of Artificial Intelligence}
\newcommand{\homeworkNumber}{9}

\renewcommand{\thesection}{Exercise \homeworkNumber.\arabic{section}}


\begin{document}

\section{Phase transitions for CNF formulas}
CNF formula $
    \varphi =
    \underbrace{(\ell_{11} \lor \ell_{12} \lor ... \lor \ell_{1k})}_{\text{clause } 1}
    \land
    \underbrace{(\ell_{21} \lor \ell_{22} \lor ... \lor \ell_{2k})}_{\text{clause } 2}
    \land
    ...
    \land
    \underbrace{(\ell_{m1} \lor \ell_{m2} \lor ... \lor \ell_{mk})}_{\text{clause } m}
$\\[1em]
with $n$ variables, $m$ clauses, and $k$ variables in each clause
\begin{enumerate}[(a)]
    \item Varying $k$ while fixing $n$ and $m$:\\[0.5em]
    For large values $k$, this will lead to an
    \textbf{under-constrained region}: For every
    additional variable in a clause, the clause
    has more "possibilities" to be satisfied and
    thus, with a large number of variables in each
    clause, the entire formula is more likely to be
    satisfied.
    \item Varying $n$ while fixing $m$ and $k$:\\[0.5em]
    For large values $n$, this will lead to an
    \textbf{under-constrained region}.
    Proof sketch: Let's assume, two
    literals $l_1, l_2$ within the same clause of
    a CNF formula $\varphi_1$ are being represented
    by the same variable.
    Furthermore, let another CNF formula $\varphi_2$
    consisting of the exact same clauses with the
    only difference that $l_2$ is represented by a
    different and additional variable, then the
    clause containing $l_1, l_2$ has more
    "possibilities" to be satisfiable and thus
    $\varphi_2$ is more likely to be satisiable. 
\end{enumerate}

\section{}
\begin{enumerate}[(a)]
    \item STRIPS planning task $\Pi = \langle
        V, I, G, A
    \rangle$ with
    \begin{itemize}
        \item $V = 
        \text{loc}_{\text{MB}},
        \text{loc}_{\text{OC}},
        \text{loc}_{\text{TM}},
        \text{belly}_{\text{empty}},
        \text{belly}_{\text{icecream}},
        \text{temp}_{\text{cold}}
        \text{temp}_{\text{warm}}
        $
        \item $I =
        \text{loc}_{\text{MB}},
        \text{belly}_{\text{empty}},
        \text{temp}_{\text{warm}}
        $
        \item $G = \{
            \text{loc}_{\text{OC}},
            \text{temp}_{\text{cold}},
            \text{belly}_\text{icecream}
        \}$
        \item $A =
        \{ 
           \{ \text{move}_{X,Y} \mid X, Y \in \{ \text{MB, OC, TM} \}, X \neq Y \},\\
           \text{swim}_\text{TM, MB},\\
           \text{eat}_\text{MB},\\
           \text{eat}_\text{OC}
        \}
        $
    \end{itemize}
    where
    \begin{itemize}
        \item $pre(\text{move}_{X,Y}) = \{ \text{loc}_X \},
        \ \forall X, Y \in \{ \text{MB, OC, TM} \}$,
        \item $add(\text{move}_{X,Y}) = \{ \text{loc}_Y \},
        \ \forall X, Y \in \{ \text{MB, OC, TM} \}$,
        \item $del(\text{move}_{X,Y}) = \{ \text{loc}_X \},
        \ \forall X, Y \in \{ \text{MB, OC, TM} \}$,
        \item $cost(\text{move}_{X,Y}) = 1,
        \ \forall X, Y \in \{ \text{MB, OC, TM} \}$,
        \item $pre(\text{swim}_\text{TM, MB}) =
        \{ \text{loc}_\text{TM} \}$,
        \item $add(\text{swim}_\text{TM, MB}) =
        \{ \text{loc}_\text{MB} \}$,
        \item $del(\text{swim}_\text{TM, MB}) =
        \{ \text{loc}_\text{TM} \}$,
        \item $cost(\text{swim}_\text{TM, MB}) = 2$,
        \item $pre(\text{eat}_\text{MB}) =
        \{ \text{loc}_\text{MB}, \text{belly}_\text{empty} \}$,
        \item $add(\text{eat}_\text{MB}) =
        \{ \text{belly}_\text{icecream} \}$,
        \item $del(\text{eat}_\text{MB}) =
        \{ \text{belly}_\text{empty} \}$,
        \item $cost(\text{eat}_\text{MB}) = 1$,
        \item $pre(\text{eat}_\text{OC}) =
        \{ \text{loc}_\text{OC}, \text{belly}_\text{empty} \}$,
        \item $add(\text{eat}_\text{OC}) =
        \{ \text{belly}_\text{icecream} \}$,
        \item $del(\text{eat}_\text{OC}) =
        \{ \text{belly}_\text{empty} \}$,
        \item $cost(\text{eat}_\text{OC}) = 1$
    \end{itemize}
    The cost for the actions represent the estimated time needed for
    the respective action. When swimming down the Rhine one also needs
    time to change, so this is more expensive than just moving by feet.
    Additionally, in this scenario, time is the most decisive
    factor because you really don't want to be late for the exam.
    \item $\pi = \langle
        \text{move}_\text{MB, TM},
        \text{swim}_\text{TM, MB},
        \text{move}_\text{MB, OC},
        \text{eat}_\text{OC}
    \rangle$\\[0.5em]
    $cost(\pi) = 1 + 2 + 1 + 1 = 5$
\end{enumerate}

\section{}
\begin{enumerate}[(a)]
    \item Graphical representation of the problem:\\[0.5em]
        \begin{tikzpicture}[->,>=stealth',auto,node distance=3.5cm]
        \node[state] (A) {altstadt};
        \node[state, initial, initial text=, right of=A] (K) {kneiphof};
        \node[state, right of=K] (L) {lomse};
        \node[state, below of=K] (V) {vorstadt};
        \path[-, bend right] (A) edge node {} (K);
        \path[-, bend left] (A) edge node {} (K);
        \path[-, bend right] (K) edge node {} (V);
        \path[-, bend left] (K) edge node {} (V);
        \path[-, bend left] (A) edge node {} (L);
        \path[-] (K) edge node {} (L);
        \path[-] (V) edge node {} (L);
    \end{tikzpicture}
    \item Greedy best-first search on
    \texttt{koenigsberg-problem.pddl} with
    \texttt{fast-downward}. Result:
    \begin{enumerate}[]
        \item Total time: 0.021764s
        \item Expanded 45 state(s)
        \item $\rightarrow$ Since no solution is found,
        we cannot discuss whether it is optimal or not.
    \end{enumerate}
    \item By commenting out line 24 in
    \texttt{bridges-once-domain.pddl}:\\
    \texttt{(bridge-has-not-been-used ?b)}\\
    we can prevent the algorithm to check whether
    the bridge has been already used.
    Consequently, the algorithm can use the same
    bridge more than once.
    \begin{enumerate}[]
        \item Total time: 0.024720s
        \item Expanded 10 state(s).
    \end{enumerate}
    \texttt{fast-downward} finds a solution, thus,
    the problem is now solvable:
    \begin{enumerate}[]
        \item cross-bridge koettelbruecke kneiphof vorstadt (1)
        \item cross-bridge gruene-bruecke vorstadt kneiphof (1)
        \item cross-bridge schmiedebruecke kneiphof altstadt (1)
        \item cross-bridge holzbruecke altstadt lomse (1)
        \item cross-bridge honigbruecke lomse kneiphof (1)
        \item cross-bridge gruene-bruecke kneiphof vorstadt (1)
        \item cross-bridge hohe-bruecke vorstadt lomse (1)
        \item cross-bridge holzbruecke lomse altstadt (1)
        \item cross-bridge kraemerbruecke altstadt kneiphof (1)
    \end{enumerate}
    \item Another problem instance of the bridges domain:
     \begin{enumerate}[]
        \item Total time: 0.032434s
        \item Expanded 14 state(s)
     \end{enumerate}
    The solution is optimal, because in this problem domain,
    every solution is optimal: All edges must be
    used exactly once, so every solution of a problem
    instance has the same length as all solutions of
    this instance.\\
    Since the problem has at least one solution, it is
    solvable.\\
    Plan:
    \begin{enumerate}[]
        \item cross-bridge v13 n1 n3 (1)
        \item cross-bridge v23 n3 n2 (1)
        \item cross-bridge v26 n2 n6 (1)
        \item cross-bridge v36 n6 n3 (1)
        \item cross-bridge v38 n3 n8 (1)
        \item cross-bridge v18 n8 n1 (1)
        \item cross-bridge v14 n1 n4 (1)
        \item cross-bridge v46 n4 n6 (1)
        \item cross-bridge v68 n6 n8 (1)
        \item cross-bridge v48 n8 n4 (1)
        \item cross-bridge v47 n4 n7 (1)
        \item cross-bridge v57 n7 n5 (1)
        \item cross-bridge v15 n5 n1 (1)
    \end{enumerate}
\end{enumerate}

\section{}
% Specify a STRIPS planning task Π which is
% unsolvable butwhose delete relaxation
% Π+ is solvable.
We will specify a STRIPS planning task $\Pi$ which
is unsolvable but whose delete relaxation $\Pi^+$
is solvable:\\[0.5em]
$\Pi = \langle
    V, I, G, A
\rangle$ with
\begin{itemize}
    \item $V = \{ A, B, C \}$
    \item $I = \{ A, C \}$
    \item $G = \{ B, C \}$
    \item $A = \{ a \}$ with
    \begin{itemize}
        \item $pre(a) = \{ A, C \}$
        \item $add(a) = \{ B \}$
        \item $del(a) = \{ C \}$
        \item $cost(a) = 1$
    \end{itemize}
\end{itemize}

\section*{Statement on scientific integrity}
We solved the entire exercise sheet on our own.

\end{document}
